import 'package:flutter/material.dart';

class IconCardInitWidget extends StatelessWidget {
  const IconCardInitWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.home,
      size: 50,
      color: Colors.grey,
    );
  }
}
